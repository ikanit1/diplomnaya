<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/task_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="25sp"
        android:textColor="@android:color/black" /> <!-- Устанавливаем по умолчанию невидимой --><!-- Виджет TextView для отображения текста задачи -->
    <TextView
        android:id="@+id/task_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textColor="@android:color/black" />

    <!-- Виджет TextView для отображения заголовка задачи -->


    <!-- Линейный макет для кнопок удаления и редактирования задачи -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <!-- Виджет TextView для отображения даты и времени создания задачи -->
        <TextView
            android:id="@+id/task_date_time"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="14sp"
            android:textColor="@android:color/black" />

        <!-- Виджет ImageView для отображения важности задачи -->
        <ImageView
            android:id="@+id/image_star"
            android:layout_width="16sp"
            android:layout_height="16sp"
            android:layout_gravity="end"
            android:layout_marginStart="8dp"
            android:src="@android:drawable/btn_star_big_on"
            android:visibility="gone" /> <!-- Устанавливаем по умолчанию невидимой -->

        <!-- Кнопка редактирования задачи -->
        <ImageButton
            android:id="@+id/button_edit_task"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="Edit"
            android:src="@android:drawable/ic_menu_edit" />

        <!-- Кнопка удаления задачи -->
        <ImageButton
            android:id="@+id/button_delete_task"
            android:layout_width="41dp"
            android:layout_height="match_parent"
            android:contentDescription="Delete"
            android:src="@android:drawable/ic_menu_delete"
            tools:src="@android:drawable/ic_menu_delete" />

    </LinearLayout>
</LinearLayout>
